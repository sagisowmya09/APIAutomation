# Override path with -Dconfig.file=/abs/path/config.properties or CONFIG_FILE env.
# Individual keys can still be overridden via -D / env (see README).

# Default Restful Booker base URI
baseUri=https://restful-booker.herokuapp.com

# Default credentials for POST /auth
auth.username=admin
auth.password=password123

# HTTP timeouts (ms) — generous defaults for Jenkins agents
connectTimeoutMs=30000
readTimeoutMs=60000
